# Project source documents

- `PARAMOUNT_BUILD_PLAN.md` — the original build plan (locked design tokens + content).
- `PARAMOUNT_CONTENT.md` — locked content + palette (source of truth, sampled from the brand deck).
- `PARAMOUNT_SCROLL_UI_PROMPT.md` — the scroll-first UI / animation layer spec.
- `original-logo.png` — the client's original logo artwork (source of `/public/brand/*` crops).
- `product-list.pdf` — the client's product & requirement list.

NOT in the repo (too large for GitHub): the brand decks `17 June AP .pdf` (3.4GB) and
`27June.pdf` (4.2GB) — kept locally in the project folder / Desktop. The extracted
design system from them lives in `DESIGN_SYNC_SPEC.md` at the repo root.
