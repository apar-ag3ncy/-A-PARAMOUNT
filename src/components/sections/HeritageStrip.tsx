import type { ReactNode } from "react";
// STUB — implement per PARAMOUNT_BUILD_PLAN.md §10 / PARAMOUNT_SCROLL_UI_PROMPT.md
export default function HeritageStrip({ children }: { children?: ReactNode }) {
  return <>{children ?? null}</>;
}
