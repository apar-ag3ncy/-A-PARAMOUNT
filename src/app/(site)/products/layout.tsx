import type { ReactNode } from "react";
// STUB — breadcrumb + category rail (PARAMOUNT_BUILD_PLAN.md §7).
export default function ProductsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
